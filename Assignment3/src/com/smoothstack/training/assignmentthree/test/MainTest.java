package com.smoothstack.training.assignmentthree.test;

import static org.junit.Assert.assertEquals;

import org.junit.Test;
import org.junit.runner.RunWith;
import org.junit.runners.Suite;

import com.smoothstack.training.assignmentthree.Main;


public class MainTest {

	//There are no methods aside from the static void main. 
	//All it does is call a static function in FileReaderService to load in the files,
	//then construct the MainMenu object.
}
